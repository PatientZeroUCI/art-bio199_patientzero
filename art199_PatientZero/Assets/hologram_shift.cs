using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using VRTK.Controllables.PhysicsBased;

public class hologram_shift : MonoBehaviour
{
   // public VRTK.VRTK_InteractableObject sliderLocation = VRTK_PhysicsSlider.GetControlInteractableObject();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
